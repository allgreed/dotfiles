#!/usr/bin/env bash
# TODO: bug: sudo bash -c echo 5 fails miserably
# TODO: maitain the environment with -m
if &>/dev/null which 'doas'; then
    doas $@
else
    # this is needed to break the inifinite loop in case
    # there isn't another 'sudo' earlier in the PATH
    /bin/sudo $@ 
fi

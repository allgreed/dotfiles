---
- hosts: all
  tasks:
    - name: install pip
      package:
        name: "{{ item }}"
        state: latest
      become: yes 
      loop:
        - sudo
        - python-pip

    - name: install latest ansible
      pip:
        state: latest
        name: ansible

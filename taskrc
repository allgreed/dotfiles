# [Created by task 2.5.3 1/14/2022 15:58:04]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=      -- By specifying no value, this means no default

# Use the command 'task show' to see all defaults and overrides
data.location=~/.task

dateformat=d.M
weekstart=sunday

urgency.user.tag.urgent.coefficient=9
urgency.user.tag.important.coefficient=3

include /nix/store/jzx3yx4zlygdnrwzma8nlzvh234zr6l1-taskwarrior-2.5.3/share/doc/task/rc/solarized-light-256.theme
# otherwise it's less visible which kindof misses the point
color.tagged=

context.out=-home
report.next.filter=+PENDING -BLOCKED limit:3

report.pending.description=All actionable tasks
report.pending.columns=id,start.age,entry.age,depends,priority,project,tags,recur,scheduled.countdown,due.relative,until.remaining,description,urgency
report.pending.labels=ID,Active,Age,Deps,P,Project,Tag,Recur,S,Due,Until,Description,Urg
report.pending.sort=urgency-
report.pending.filter=+PENDING -BLOCKED
